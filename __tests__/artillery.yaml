config:
  target: "{{ $processEnvironment.URL }}"
  phases:
    - duration: 2 # seconds
      arrivalRate: 2 # requests per seconds

scenarios:
  - name: "Connect"
    flow:
      - get:
          url: "/"
